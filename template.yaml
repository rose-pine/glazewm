# name: $name
base: &base "$base"
surface: &surface "$surface"
overlay: &overlay "$overlay"
inactive: &inactive "$muted"
muted: &muted "$muted"
subtle: &subtle "$subtle"
text: &text "$text"
love: &love "$love"
gold: &gold "$gold"
rose: &rose "$rose"
pine: &pine "$pine"
foam: &foam "$foam"
iris: &iris "$iris"
highlight_low: &highlight_low "$highlightLow"
highlight_med: &highlight_med "$highlightMed"
highlight_high: &highlight_high "$highlightHigh"
